{
  "name": "ssg",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "npm run build && concurrently \"npm run dev:html\" \"npm run dev:sass\" \"npm run dev:js\" \"npm start\"",
    "dev:js": "webpack --watch",
    "dev:html": "chokidar 'config.json' 'src/**/*' 'posts/**/*.md' -c 'npm run build:html'  -i 'src/resources/**/*'",
    "dev:sass": "npm run build:sass -- --watch src/resources/**/*.scss",
    "build": "npm run clean && npm run build:html && npm run build:js && npm run build:sass && npm run build:static",
    "build:static": "cp -r src/resources/img dist",
    "build:sass": "node-sass src/resources/scss/screen.scss dist/css/screen.css",
    "build:html": "rm -rf dist/**/*.html && node src/lib/build.js",
    "build:js": "webpack",
    "clean": "ls dist | grep -v .htaccess | xargs -I {} rm -rf dist/{}",
    "start": "http-server -p 1823 ./dist -c-1",
    "test": "mocha --opts test/mocha.opts"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dateformat": "^1.0.12",
    "fs-extra": "^0.30.0",
    "get-set": "git+ssh://git@github.com/scottyeck/getset.git",
    "lodash": "4.11.2",
    "lodash.throttle": "^4.0.1",
    "meta-marked": "0.4.1",
    "node-sass": "3.7.0",
    "pug": "^2.0.0-alpha6",
    "sassline": "^2.1.2",
    "typesettings": "^3.0.1"
  },
  "devDependencies": {
    "chai": "3.5.0",
    "chokidar-cli": "^1.2.0",
    "concurrently": "^2.1.0",
    "glob": "7.0.3",
    "grunt": "1.0.1",
    "grunt-cli": "1.2.0",
    "http-server": "^0.9.0",
    "jquery": "^2.2.4",
    "lodash.debounce": "^4.0.6",
    "mocha": "2.4.5",
    "rfr": "1.2.3",
    "sinon": "^1.17.4",
    "sinon-chai": "^2.8.0",
    "webpack": "^1.13.1"
  }
}
